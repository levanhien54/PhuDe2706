httpx==0.27.0
ffmpeg-python==0.2.0
librosa==0.10.2
soundfile==0.12.1
pedalboard>=0.9.0
pydantic==2.13.4
python-dotenv==1.0.1
# Text detection for the xóa-chữ (text-removal) stage. EasyOCR's CRAFT detector is pure PyTorch,
# so it runs on the working cuDNN-9 GPU (paddlepaddle needs cuDNN 8 which is broken on this stack).
# Detector weight (craft_mlt_25k.pth) is vendored in models/easyocr/ for offline use.
easyocr==1.7.2
opencv-python>=4.8.0
pydantic-settings==2.3.4
structlog==24.2.0
respx==0.21.1
pytest==8.2.2
pytest-asyncio==0.23.7
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9
# Vietnamese text normalization for TTS (numbers, acronyms) — imported by orchestrator/text_normalize.py
vietnormalizer==0.2.3
